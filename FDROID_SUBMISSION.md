# Jet Note — F-Droid submission preparation

Application ID: `com.ingeniousidea.space`
Version: `8.0.6` (`versionCode 176`)
Build system: Android Gradle Plugin 8.2.2 / Java 17
Minimum Android: API 23
Target Android: API 34

## Added to this package

- `fastlane/metadata/android/en-US/` — English store description and changelog.
- `fastlane/metadata/android/zh-CN/` — Simplified Chinese store description and changelog.
- `fastlane/metadata/android/*/images/icon.png` — store icon generated from the app's existing vector icon.
- `fdroid/metadata/com.ingeniousidea.space.yml` — fdroiddata metadata draft.

## Items that cannot be truthfully filled in from this ZIP

### 1. FOSS license — REQUIRED

No `LICENSE`/`COPYING` file was present in the supplied project. F-Droid requires a recognized free/open-source license. The copyright holder must choose the license; it should not be guessed or added on their behalf.

After choosing a license:
1. Add its full license text as `LICENSE` at repository root.
2. Replace `GPL-3.0-only` in the fdroid metadata with the matching SPDX/F-Droid identifier (for example `Apache-2.0`, `GPL-3.0-only`, etc.).

### 2. Source repository URL — REQUIRED

The ZIP contains no Git remote information. Replace `lei1737524550/Jet-Note` in `fdroid/metadata/com.ingeniousidea.space.yml` with the public source repository owner/name.

### 3. Release Git tag — REQUIRED

The build metadata expects tag `v8.0.6`. Ensure the exact source corresponding to version 8.0.6 / versionCode 176 is committed and tagged:

```sh
git tag v8.0.6
git push origin v8.0.6
```

If your existing tag has another name, change `commit:` in the YAML instead of creating a duplicate tag.

### 4. Real screenshots — RECOMMENDED / expected metadata

No screenshots were present in the supplied ZIP. Capture actual Jet Note screens and put them at:

- `fastlane/metadata/android/en-US/images/phoneScreenshots/1.png`
- `fastlane/metadata/android/en-US/images/phoneScreenshots/2.png`

You may reuse the same screenshots under `zh-CN` if the screenshots are language-neutral; otherwise capture localized screenshots.

### 5. Gradle Wrapper — strongly recommended

The supplied ZIP has no `gradlew`, `gradlew.bat`, or `gradle/wrapper/` files. Commit the Gradle Wrapper generated by the Gradle version compatible with Android Gradle Plugin 8.2.2. F-Droid builds from source and reproducible build tooling is important.

## Dependency scan notes

`app/build.gradle` currently declares no app libraries in `dependencies {}`. No `.jar`, `.aar`, `.so`, or APK binary blobs were found in the supplied project. The project does use Google's Maven repository for the Android Gradle Plugin; this is normal Android build infrastructure and is separate from bundling Google Play Services/Firebase in the app.

The source contains links/web functionality involving third-party websites. F-Droid reviewers may classify reliance on non-free network services as an AntiFeature depending on how the feature behaves. The app should remain useful without proprietary services where possible, and any such dependency should be disclosed during review.

## Before submitting

1. Add a valid FOSS `LICENSE` file.
2. Fill the repository URLs and license in `fdroid/metadata/com.ingeniousidea.space.yml`.
3. Add the Gradle Wrapper.
4. Add real screenshots.
5. Confirm/tag the 8.0.6 source as `v8.0.6` (or update `commit:` to your actual tag/commit).
6. Put the Fastlane metadata in the public upstream repository and push it.
7. Fork `fdroid/fdroiddata` on GitLab.
8. Copy `fdroid/metadata/com.ingeniousidea.space.yml` to `fdroiddata/metadata/com.ingeniousidea.space.yml`.
9. Run `fdroid rewritemeta com.ingeniousidea.space`, `fdroid lint com.ingeniousidea.space`, and a test build/CI.
10. Submit the fdroiddata branch as a New App merge request.

Do not submit the draft YAML while any `REPLACE_ME` value remains.
