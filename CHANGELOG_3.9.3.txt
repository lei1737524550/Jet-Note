Jet Note 3.9.3 - Shared Top Bar Spec

- New Post remains the reference geometry.
- Added config.json -> top_bar as the single source of truth for top-bar geometry and visual constants.
- Added js/components/jet_topbar.js so HTML top bars read the shared spec.
- Added JetTopBarSpec.java so native Dictionary/Sentences tool overlays read the same spec.
- Removed duplicated native toolbar geometry constants from DictionaryController.
- Unified height, control height, title size, side axes, radius, border colors, background and action colors.
- Native Get Audio is positioned by its center axis, not by its own width.
- Added the same bottom divider treatment to native tool top bars.
- Tools page continues to use the reusable HTML .jet-topbar component.

Important: page-specific content remains separate. New Post uses Cancel/New Post/Post; tool pages use Back/<tool title>/Get Audio. Only the content/action changes; the layout spec is shared.
