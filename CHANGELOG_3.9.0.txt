Jet Note 3.9.0 Unified Geometry & Media Fix

Implemented:
- New Post header geometry: true centered title, mirrored Cancel/Post axes, Post aligned to toolbar center.
- Toolbar uses one 48px vertical axis for Video/Image/Audio/Tools/config_tool_1/config_tool_2.
- Added Jet Note custom scrollbar (20px lane, 300px track) that appears only when active content overflows.
- Disabled Android WebView and Dictionary WebView system scrollbars; browser scrollbars hidden while scrolling remains enabled.
- Media picker cancellation remains silent; attachment picker errors return stable error codes instead of native hardcoded Toast text.
- Added language.json keys for new visible UI strings and localized post action accessibility labels.
- Video poster/player lifecycle separated: poster remains the cover until first user tap creates the persistent player.
- Publish flow hardened with in-flight guard and button disable during commit; Draft remains the publication source.
- Preserved unified Post Action Menu behavior and shared visual metrics for normal/delete-confirm states.

Validation performed:
- JavaScript syntax checks passed for modified JS files.
- english.json parsed successfully.
- Android Gradle build was not executed because gradle/gradlew is unavailable in this environment.
