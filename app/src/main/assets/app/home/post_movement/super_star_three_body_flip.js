(function (global) {
  'use strict';
  // Marker module only. The v7 three-body execution path in posts.js is kept
  // byte-for-byte behaviorally intact because it is the validated reference.
  global.JetNoteSuperStarThreeBodyFlip = { preserveLegacyV7: true };
})(window);
