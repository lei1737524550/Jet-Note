# res/drawable-nodpi/make_round_png.json — 双语逐行备注 / Bilingual line notes

原文件保持不变，以下备注按原文件行号对应。JSON 和第三方压缩库不插入行内注释，以避免破坏解析或依赖完整性。

The source file remains unchanged. These notes map to its original line numbers. JSON and minified third-party libraries receive sidecar notes so parsing and dependency integrity stay intact.

| 行 / Line | 原始内容 / Source | 中文备注 | English note |
| ---: | --- | --- | --- |
| 1 | <code>{</code> | 开始一个 JSON 对象或数组。 | Begins a JSON object or array. |
| 2 | <code>  "source": "../drawable/icon.png",</code> | 配置键“source”及其对应值；JSON 保持无注释以便程序解析。 | Sets configuration key “source” and its value; JSON remains comment-free so the app can parse it. |
| 3 | <code>  "output": "icon_rounded.png",</code> | 配置键“output”及其对应值；JSON 保持无注释以便程序解析。 | Sets configuration key “output” and its value; JSON remains comment-free so the app can parse it. |
| 4 | <code>  "outputSize": 1254,</code> | 配置键“outputSize”及其对应值；JSON 保持无注释以便程序解析。 | Sets configuration key “outputSize” and its value; JSON remains comment-free so the app can parse it. |
| 5 | <code>  "cornerRadius": 250,</code> | 配置键“cornerRadius”及其对应值；JSON 保持无注释以便程序解析。 | Sets configuration key “cornerRadius” and its value; JSON remains comment-free so the app can parse it. |
| 6 | <code>  "fit": "centerCrop",</code> | 配置键“fit”及其对应值；JSON 保持无注释以便程序解析。 | Sets configuration key “fit” and its value; JSON remains comment-free so the app can parse it. |
| 7 | <code>  "format": "png"</code> | 配置键“format”及其对应值；JSON 保持无注释以便程序解析。 | Sets configuration key “format” and its value; JSON remains comment-free so the app can parse it. |
| 8 | <code>}</code> | 结束当前 JSON 对象或数组。 | Ends the current JSON object or array. |
