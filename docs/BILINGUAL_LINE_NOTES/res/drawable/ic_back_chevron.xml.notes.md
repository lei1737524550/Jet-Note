# res/drawable/ic_back_chevron.xml — 双语逐行备注 / Bilingual line notes

原文件保持不变，以下备注按原文件行号对应。JSON 和第三方压缩库不插入行内注释，以避免破坏解析或依赖完整性。

The source file remains unchanged. These notes map to its original line numbers. JSON and minified third-party libraries receive sidecar notes so parsing and dependency integrity stay intact.

| 行 / Line | 原始内容 / Source | 中文备注 | English note |
| ---: | --- | --- | --- |
| 1 | <code>&lt;?xml version="1.0" encoding="utf-8"?&gt;</code> | 声明 XML 文件格式和编码。 | Declares the XML file format and encoding. |
| 2 | <code>&lt;vector xmlns:android="http://schemas.android.com/apk/res/android"</code> | 定义或配置 HTML/XML 元素 <vector> 及其属性。 | Defines or configures the HTML/XML <vector> element and its attributes. |
| 3 | <code>    android:width="24dp"</code> | 组成 HTML/XML 文档结构或资源配置。 | Forms part of the HTML/XML document structure or resource configuration. |
| 4 | <code>    android:height="24dp"</code> | 组成 HTML/XML 文档结构或资源配置。 | Forms part of the HTML/XML document structure or resource configuration. |
| 5 | <code>    android:viewportWidth="24"</code> | 组成 HTML/XML 文档结构或资源配置。 | Forms part of the HTML/XML document structure or resource configuration. |
| 6 | <code>    android:viewportHeight="24"&gt;</code> | 组成 HTML/XML 文档结构或资源配置。 | Forms part of the HTML/XML document structure or resource configuration. |
| 7 | <code>    &lt;path</code> | 定义或配置 HTML/XML 元素 <path> 及其属性。 | Defines or configures the HTML/XML <path> element and its attributes. |
| 8 | <code>        android:fillColor="@android:color/transparent"</code> | 组成 HTML/XML 文档结构或资源配置。 | Forms part of the HTML/XML document structure or resource configuration. |
| 9 | <code>        android:strokeColor="#222222"</code> | 组成 HTML/XML 文档结构或资源配置。 | Forms part of the HTML/XML document structure or resource configuration. |
| 10 | <code>        android:strokeWidth="2.8"</code> | 组成 HTML/XML 文档结构或资源配置。 | Forms part of the HTML/XML document structure or resource configuration. |
| 11 | <code>        android:strokeLineCap="round"</code> | 组成 HTML/XML 文档结构或资源配置。 | Forms part of the HTML/XML document structure or resource configuration. |
| 12 | <code>        android:strokeLineJoin="round"</code> | 组成 HTML/XML 文档结构或资源配置。 | Forms part of the HTML/XML document structure or resource configuration. |
| 13 | <code>        android:pathData="M15,6 L9,12 L15,18" /&gt;</code> | 组成 HTML/XML 文档结构或资源配置。 | Forms part of the HTML/XML document structure or resource configuration. |
| 14 | <code>&lt;/vector&gt;</code> | 关闭前面打开的 HTML/XML 元素。 | Closes a previously opened HTML/XML element. |
