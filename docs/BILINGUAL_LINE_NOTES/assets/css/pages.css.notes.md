# assets/css/pages.css — 双语逐行备注 / Bilingual line notes

原文件保持不变，以下备注按原文件行号对应。JSON 和第三方压缩库不插入行内注释，以避免破坏解析或依赖完整性。

The source file remains unchanged. These notes map to its original line numbers. JSON and minified third-party libraries receive sidecar notes so parsing and dependency integrity stay intact.

| 行 / Line | 原始内容 / Source | 中文备注 | English note |
| ---: | --- | --- | --- |
| 1 | <code>* {</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 2 | <code>  box-sizing: border-box;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 3 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 4 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 5 | <code>button {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 6 | <code>  font: inherit;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 7 | <code>  color: inherit;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 8 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 9 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 10 | <code>input {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 11 | <code>  font: inherit;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 12 | <code>  color: inherit;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 13 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 14 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 15 | <code>button {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 16 | <code>  -webkit-tap-highlight-color: transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 17 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 18 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 19 | <code>.name.editing {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 20 | <code>  border-bottom: 2px solid #2d8cff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 21 | <code>  padding-bottom: 3px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 22 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 23 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 24 | <code>.composer {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 25 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 26 | <code>  left: 30px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 27 | <code>  top: 8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 28 | <code>  width: 631px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 29 | <code>  height: 84px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 30 | <code>  border: 1.5px solid #cfd1d5;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 31 | <code>  border-radius: 20px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 32 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 33 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 34 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 35 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 36 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 37 | <code>.composer-main {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 38 | <code>  flex: 1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 39 | <code>  height: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 40 | <code>  border: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 41 | <code>  background: transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 42 | <code>  text-align: left;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 43 | <code>  padding-left: 26px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 44 | <code>  font-size: 24px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 45 | <code>  color: #9da0a4;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 46 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 47 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 48 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 49 | <code>.composer-photo {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 50 | <code>  width: 72px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 51 | <code>  height: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 52 | <code>  border: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 53 | <code>  background: transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 54 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 55 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 56 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 57 | <code>  transform: translateX(5px);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 58 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 59 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 60 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 61 | <code>.composer-photo svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 62 | <code>  width: 38px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 63 | <code>  height: 38px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 64 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 65 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 66 | <code>.post1 {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 67 | <code>  height: 397px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 68 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 69 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 70 | <code>.post2 {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 71 | <code>  height: 328px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 72 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 73 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 74 | <code>.black-card {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 75 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 76 | <code>  left: 17px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 77 | <code>  top: 96px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 78 | <code>  width: 628px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 79 | <code>  height: 207px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 80 | <code>  overflow: hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 81 | <code>  padding: 22px 26px 18px 27px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 82 | <code>  border-radius: 8px 8px 0 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 83 | <code>  background: #020202;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 84 | <code>  color: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 85 | <code>  font-size: 28px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 86 | <code>  line-height: 1.42;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 87 | <code>  letter-spacing: .2px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 88 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 89 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 90 | <code>.black-card .line2 {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 91 | <code>  margin-top: 22px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 92 | <code>  padding-left: 25px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 93 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 94 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 95 | <code>button:active {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 96 | <code>  opacity: .68;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 97 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 98 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 99 | <code>.zoom-row {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 100 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 101 | <code>  left: 65px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 102 | <code>  top: 910px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 103 | <code>  width: 561px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 104 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 105 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 106 | <code>  gap: 17px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 107 | <code>  color: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 108 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 109 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 110 | <code>.zoom-row svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 111 | <code>  width: 26px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 112 | <code>  height: 26px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 113 | <code>  flex: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 114 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 115 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 116 | <code>#zoomRange {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 117 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 118 | <code>  accent-color: #20a9ff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 119 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 120 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 121 | <code>.black-card {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 122 | <code>  display: none !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 123 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 124 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 125 | <code>.post-compose-screen {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 126 | <code>  position: fixed;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 127 | <code>  left: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 128 | <code>  top: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 129 | <code>  width: 100vw;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 130 | <code>  height: 100dvh;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 131 | <code>  z-index: 9999;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 132 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 133 | <code>  overflow: hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 134 | <code>  background: #f5f6f8;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 135 | <code>  transform: none !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 136 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 137 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 138 | <code>.post-compose-screen.open {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 139 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 140 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 141 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 142 | <code>.post-compose-header {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 143 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 144 | <code>  left: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 145 | <code>  top: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 146 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 147 | <code>  height: var(--jet-toolbar-height);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 148 | <code>  z-index: 5;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 149 | <code>  display: grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 150 | <code>  grid-template-columns: var(--jet-toolbar-side-slot) minmax(0, 1fr) var(--jet-toolbar-side-slot);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 151 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 152 | <code>  padding: 0 var(--jet-toolbar-horizontal-padding);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 153 | <code>  background: #f8f9fa;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 154 | <code>  border-bottom: 1px solid #eceef1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 155 | <code>  box-shadow: 0 2px 4px rgba(0,0,0,.10);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 156 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 157 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 158 | <code>.post-compose-title {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 159 | <code>  grid-column: 2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 160 | <code>  min-width: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 161 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 162 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 163 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 164 | <code>  font-size: var(--jet-toolbar-title-size);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 165 | <code>  font-weight: 400;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 166 | <code>  line-height: 1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 167 | <code>  white-space: nowrap;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 168 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 169 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 170 | <code>.post-compose-cancel {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 171 | <code>  grid-column: 1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 172 | <code>  justify-self: start;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 173 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 174 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 175 | <code>.post-compose-publish {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 176 | <code>  grid-column: 3;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 177 | <code>  justify-self: end;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 178 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 179 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 180 | <code>.post-compose-cancel,</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 181 | <code>.post-compose-publish {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 182 | <code>  position: static;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 183 | <code>  display: inline-flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 184 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 185 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 186 | <code>  min-height: var(--jet-toolbar-control-height);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 187 | <code>  height: var(--jet-toolbar-control-height);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 188 | <code>  padding: 0 var(--jet-toolbar-control-padding-x);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 189 | <code>  border: 1px solid #c8cbd0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 190 | <code>  border-radius: var(--jet-toolbar-control-radius);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 191 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 192 | <code>  font-size: var(--jet-toolbar-control-size);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 193 | <code>  font-weight: 400;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 194 | <code>  line-height: 1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 195 | <code>  white-space: nowrap;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 196 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 197 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 198 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 199 | <code>.post-compose-cancel {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 200 | <code>  min-width: 64px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 201 | <code>  color: #222;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 202 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 203 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 204 | <code>.post-compose-publish {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 205 | <code>  min-width: 56px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 206 | <code>  color: #1599e8;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 207 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 208 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 209 | <code>.post-compose-body {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 210 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 211 | <code>  left: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 212 | <code>  right: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 213 | <code>  top: var(--jet-toolbar-height);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 214 | <code>  bottom: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 215 | <code>  margin-top: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 216 | <code>  overflow-y: auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 217 | <code>  -webkit-overflow-scrolling: touch;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 218 | <code>  overscroll-behavior: contain;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 219 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 220 | <code>  padding: 22px 20px 48px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 221 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 222 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 223 | <code>#postComposerText {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 224 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 225 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 226 | <code>  min-height: 300px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 227 | <code>  scroll-margin-top: 70px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 228 | <code>  resize: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 229 | <code>  border: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 230 | <code>  outline: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 231 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 232 | <code>  color: #111;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 233 | <code>  font: inherit;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 234 | <code>  font-size: 28px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 235 | <code>  line-height: 1.55;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 236 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 237 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 238 | <code>#postComposerText::placeholder {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 239 | <code>  color: #a5a8ad;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 240 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 241 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 242 | <code>.post-compose-count {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 243 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 244 | <code>  right: 28px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 245 | <code>  bottom: 17px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 246 | <code>  color: #a3a6aa;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 247 | <code>  font-size: 18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 248 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 249 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 250 | <code>.settings-screen {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 251 | <code>  position: fixed;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 252 | <code>  left: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 253 | <code>  top: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 254 | <code>  width: 100vw;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 255 | <code>  height: 100dvh;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 256 | <code>  z-index: 10000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 257 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 258 | <code>  overflow: hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 259 | <code>  background: #f5f6f8;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 260 | <code>  color: #111;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 261 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 262 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 263 | <code>.settings-screen.open {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 264 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 265 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 266 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 267 | <code>.settings-header {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 268 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 269 | <code>  left: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 270 | <code>  top: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 271 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 272 | <code>  height: 64px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 273 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 274 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 275 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 276 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 277 | <code>  border-bottom: 1px solid #eceef1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 278 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 279 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 280 | <code>.settings-title {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 281 | <code>  font-size: 20px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 282 | <code>  font-weight: 600;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 283 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 284 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 285 | <code>.settings-back {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 286 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 287 | <code>  left: 8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 288 | <code>  top: 6px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 289 | <code>  width: 52px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 290 | <code>  height: 52px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 291 | <code>  border: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 292 | <code>  background: transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 293 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 294 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 295 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 296 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 297 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 298 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 299 | <code>.settings-back svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 300 | <code>  width: 30px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 301 | <code>  height: 30px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 302 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 303 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 304 | <code>.settings-body {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 305 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 306 | <code>  left: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 307 | <code>  right: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 308 | <code>  top: 64px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 309 | <code>  bottom: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 310 | <code>  overflow-y: auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 311 | <code>  padding: 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 312 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 313 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 314 | <code>.settings-card {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 315 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 316 | <code>  border: 1px solid rgb(200,200,200);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 317 | <code>  border-radius: 15px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 318 | <code>  overflow: hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 319 | <code>  margin-bottom: 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 320 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 321 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 322 | <code>.settings-row-title {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 323 | <code>  padding: 18px 18px 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 324 | <code>  font-size: 18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 325 | <code>  font-weight: 600;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 326 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 327 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 328 | <code>.settings-row-title-compact {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 329 | <code>  padding-top: 4px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 330 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 331 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 332 | <code>.settings-option-grid {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 333 | <code>  display: grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 334 | <code>  grid-template-columns: repeat(2, minmax(0, 1fr));</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 335 | <code>  gap: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 336 | <code>  padding: 0 12px 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 337 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 338 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 339 | <code>.settings-choice {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 340 | <code>  position: relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 341 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 342 | <code>  min-width: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 343 | <code>  min-height: 74px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 344 | <code>  padding: 11px 34px 11px 14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 345 | <code>  border: 1px solid #c8ccd1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 346 | <code>  border-radius: 13px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 347 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 348 | <code>  text-align: left;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 349 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 350 | <code>  transition: background-color .16s ease, border-color .16s ease, box-shadow .16s ease;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 351 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 352 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 353 | <code>.settings-choice.active {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 354 | <code>  border-color: #74b9e6;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 355 | <code>  background: #eaf6ff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 356 | <code>  box-shadow: inset 0 0 0 1px rgba(52,155,219,.12);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 357 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 358 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 359 | <code>.settings-choice-main {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 360 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 361 | <code>  overflow-wrap: anywhere;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 362 | <code>  font-size: 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 363 | <code>  font-weight: 500;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 364 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 365 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 366 | <code>.settings-choice-sub {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 367 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 368 | <code>  margin-top: 4px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 369 | <code>  overflow-wrap: anywhere;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 370 | <code>  font-size: 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 371 | <code>  color: #999;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 372 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 373 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 374 | <code>.settings-choice-check {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 375 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 376 | <code>  right: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 377 | <code>  top: 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 378 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 379 | <code>  font-size: 17px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 380 | <code>  color: #1599e8;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 381 | <code>  font-weight: 700;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 382 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 383 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 384 | <code>.settings-choice.active .settings-choice-check {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 385 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 386 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 387 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 388 | <code>.settings-action-choice {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 389 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 390 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 391 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 392 | <code>  padding: 11px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 393 | <code>  text-align: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 394 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 395 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 396 | <code>.settings-action-choice.cache-attention {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 397 | <code>  border-color: #87cba0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 398 | <code>  background: #eaf8ee;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 399 | <code>  box-shadow: inset 0 0 0 1px rgba(61, 157, 88, .10);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 400 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 401 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 402 | <code>.settings-choice-cache-arrow {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 403 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 404 | <code>  top: 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 405 | <code>  right: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 406 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 407 | <code>  color: #29934d;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 408 | <code>  font-size: 18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 409 | <code>  font-weight: 700;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 410 | <code>  line-height: 1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 411 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 412 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 413 | <code>.settings-action-choice.cache-attention .settings-choice-cache-arrow {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 414 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 415 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 416 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 417 | <code>/* Demo is a display-only session: no composer or persistent-data backup UI. */</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 418 | <code>html.demo-read-only #mainComposerArea,</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 419 | <code>html.demo-read-only .feed-composer-wrap {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 420 | <code>  display: none !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 421 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 422 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 423 | <code>.settings-status {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 424 | <code>  min-height: 18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 425 | <code>  margin: 0 18px 14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 426 | <code>  color: #4e6678;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 427 | <code>  font-size: 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 428 | <code>  line-height: 1.45;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 429 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 430 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 431 | <code>.settings-status.error {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 432 | <code>  color: #b43b3b;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 433 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 434 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 435 | <code>.shape-preview {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 436 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 437 | <code>  width: 28px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 438 | <code>  height: 28px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 439 | <code>  margin-bottom: 7px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 440 | <code>  border: 2px solid #315f98;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 441 | <code>  background: #edf5fb;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 442 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 443 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 444 | <code>.shape-preview.round { border-radius: 50%; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 445 | <code>.shape-preview.square { border-radius: 5px; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 446 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 447 | <code>.rgb-editor {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 448 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 449 | <code>  margin: 0 12px 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 450 | <code>  padding: 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 451 | <code>  border: 1px solid #d8dde2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 452 | <code>  border-radius: 13px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 453 | <code>  background: #f8fafb;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 454 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 455 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 456 | <code>.rgb-editor.open { display: block; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 457 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 458 | <code>.rgb-fields {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 459 | <code>  display: grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 460 | <code>  grid-template-columns: repeat(3, minmax(0, 1fr));</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 461 | <code>  gap: 8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 462 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 463 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 464 | <code>.rgb-fields label {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 465 | <code>  display: grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 466 | <code>  grid-template-columns: 18px minmax(0, 1fr);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 467 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 468 | <code>  gap: 4px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 469 | <code>  color: #59636d;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 470 | <code>  font-size: 13px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 471 | <code>  font-weight: 600;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 472 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 473 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 474 | <code>.rgb-fields input {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 475 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 476 | <code>  min-width: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 477 | <code>  height: 38px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 478 | <code>  padding: 5px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 479 | <code>  border: 1px solid #c8ccd1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 480 | <code>  border-radius: 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 481 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 482 | <code>  text-align: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 483 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 484 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 485 | <code>.rgb-apply {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 486 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 487 | <code>  min-height: 40px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 488 | <code>  margin-top: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 489 | <code>  border: 1px solid #74b9e6;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 490 | <code>  border-radius: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 491 | <code>  background: #eaf6ff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 492 | <code>  color: #245f86;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 493 | <code>  font-weight: 600;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 494 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 495 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 496 | <code>.appearance-status,#archiveStatus {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 497 | <code>  min-height: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 498 | <code>  margin: 0 14px 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 499 | <code>  color: #68727c;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 500 | <code>  font-size: 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 501 | <code>  overflow-wrap: anywhere;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 502 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 503 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 504 | <code>.compose-media-toolbar {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 505 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 506 | <code>  flex-wrap: wrap;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 507 | <code>  gap: 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 508 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 509 | <code>  margin-top: 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 510 | <code>  padding-top: 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 511 | <code>  border-top: 1px solid #eeeeee;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 512 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 513 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 514 | <code>.compose-add-image {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 515 | <code>  height: 44px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 516 | <code>  padding: 0 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 517 | <code>  border: 1px solid #d7d7d7;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 518 | <code>  border-radius: 11px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 519 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 520 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 521 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 522 | <code>  gap: 7px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 523 | <code>  color: #333;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 524 | <code>  font-size: 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 525 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 526 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 527 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 528 | <code>.compose-add-image svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 529 | <code>  width: 25px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 530 | <code>  height: 25px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 531 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 532 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 533 | <code>.post .text-content:empty {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 534 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 535 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 536 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 537 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 538 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 539 | <code>.settings-row-subtitle {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 540 | <code>  margin-top: -6px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 541 | <code>  margin-bottom: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 542 | <code>  font-size: 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 543 | <code>  line-height: 1.35;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 544 | <code>  color: #8b949d;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 545 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
