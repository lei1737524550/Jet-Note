# assets/css/components.css — 双语逐行备注 / Bilingual line notes

原文件保持不变，以下备注按原文件行号对应。JSON 和第三方压缩库不插入行内注释，以避免破坏解析或依赖完整性。

The source file remains unchanged. These notes map to its original line numbers. JSON and minified third-party libraries receive sidecar notes so parsing and dependency integrity stay intact.

| 行 / Line | 原始内容 / Source | 中文备注 | English note |
| ---: | --- | --- | --- |
| 1 | <code>.crop-screen {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 2 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 3 | <code>  inset: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 4 | <code>  z-index: 100;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 5 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 6 | <code>  background: #000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 7 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 8 | <code>.crop-screen.open {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 9 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 10 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 11 | <code>.crop-header {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 12 | <code>  height: 82px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 13 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 14 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 15 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 16 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 17 | <code>  position: relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 18 | <code>  font-size: 28px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 19 | <code>  font-weight: 500;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 20 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 21 | <code>.crop-back {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 22 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 23 | <code>  left: 25px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 24 | <code>  top: 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 25 | <code>  width: 52px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 26 | <code>  height: 52px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 27 | <code>  padding: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 28 | <code>  border: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 29 | <code>  background: transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 30 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 31 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 32 | <code>.crop-back svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 33 | <code>  width: 34px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 34 | <code>  height: 34px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 35 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 36 | <code>.crop-body {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 37 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 38 | <code>  left: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 39 | <code>  top: 82px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 40 | <code>  width: 691px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 41 | <code>  height: 1110px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 42 | <code>  background: #000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 43 | <code>  overflow: hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 44 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 45 | <code>#cropCanvas {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 46 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 47 | <code>  left: 30px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 48 | <code>  top: 150px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 49 | <code>  width: 631px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 50 | <code>  height: 690px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 51 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 52 | <code>  touch-action: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 53 | <code>  background: #000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 54 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 55 | <code>.crop-hint {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 56 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 57 | <code>  left: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 58 | <code>  top: 865px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 59 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 60 | <code>  text-align: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 61 | <code>  color: rgba(255,255,255,.72);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 62 | <code>  font-size: 18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 63 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 64 | <code>.crop-footer {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 65 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 66 | <code>  left: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 67 | <code>  top: 1050px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 68 | <code>  bottom: auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 69 | <code>  width: 691px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 70 | <code>  height: 90px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 71 | <code>  background: #000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 72 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 73 | <code>.crop-cancel {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 74 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 75 | <code>  left: 32px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 76 | <code>  bottom: 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 77 | <code>  border: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 78 | <code>  background: transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 79 | <code>  color: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 80 | <code>  font-size: 24px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 81 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 82 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 83 | <code>.crop-done {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 84 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 85 | <code>  right: 28px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 86 | <code>  bottom: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 87 | <code>  min-width: 90px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 88 | <code>  height: 58px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 89 | <code>  padding: 0 22px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 90 | <code>  border: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 91 | <code>  border-radius: 29px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 92 | <code>  background: #1da8f5;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 93 | <code>  color: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 94 | <code>  font-size: 24px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 95 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 96 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 97 | <code>.post-action-btn:active {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 98 | <code>  background: #f5f5f5;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 99 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 100 | <code>.post-action-icon {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 101 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 102 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 103 | <code>.post-action-label {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 104 | <code>  font-weight: 500;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 105 | <code>  color: #222;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 106 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 107 | <code>.post-action-btn.delete .post-action-label {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 108 | <code>  color: #d93636;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 109 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 110 | <code>.post-action-btn.delete .post-action-icon {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 111 | <code>  stroke: #d93636;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 112 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 113 | <code>.compose-image-preview {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 114 | <code>  display: grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 115 | <code>  grid-template-columns: repeat(3,1fr);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 116 | <code>  gap: 7px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 117 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 118 | <code>.post-compose-images {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 119 | <code>  margin-top: 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 120 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 121 | <code>.compose-image-preview:empty {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 122 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 123 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 124 | <code>.compose-image-item {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 125 | <code>  position: relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 126 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 127 | <code>  aspect-ratio: 1 / 1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 128 | <code>  overflow: hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 129 | <code>  border: 1px solid #dedede;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 130 | <code>  border-radius: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 131 | <code>  background: #f2f2f2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 132 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 133 | <code>.compose-image-item img {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 134 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 135 | <code>  height: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 136 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 137 | <code>  object-fit: cover;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 138 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 139 | <code>.compose-image-remove {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 140 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 141 | <code>  right: 5px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 142 | <code>  top: 5px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 143 | <code>  width: 27px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 144 | <code>  height: 27px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 145 | <code>  padding: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 146 | <code>  border: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 147 | <code>  border-radius: 50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 148 | <code>  background: rgba(0,0,0,.65);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 149 | <code>  color: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 150 | <code>  font-size: 19px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 151 | <code>  line-height: 27px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 152 | <code>  text-align: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 153 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 154 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 155 | <code>.post-media-grid {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 156 | <code>  display: grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 157 | <code>  gap: 5px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 158 | <code>  margin-top: 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 159 | <code>  overflow: hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 160 | <code>  border-radius: 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 161 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 162 | <code>.post-media-grid.one {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 163 | <code>  grid-template-columns: 1fr;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 164 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 165 | <code>.post-media-grid.two {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 166 | <code>  grid-template-columns: repeat(2,1fr);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 167 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 168 | <code>.post-media-grid.many {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 169 | <code>  grid-template-columns: repeat(3,1fr);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 170 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 171 | <code>.post-media-grid img {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 172 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 173 | <code>  width: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 174 | <code>  height: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 175 | <code>  object-fit: cover;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 176 | <code>  background: #eee;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 177 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 178 | <code>.post-media-grid.one img {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 179 | <code>  max-height: 460px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 180 | <code>  aspect-ratio: auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 181 | <code>  object-fit: contain;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 182 | <code>  background: #f4f4f4;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 183 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 184 | <code>.post-media-grid.two img {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 185 | <code>  aspect-ratio: 1 / 1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 186 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 187 | <code>.post-media-grid.many img {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 188 | <code>  aspect-ratio: 1 / 1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 189 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 190 | <code>.post-media-grid img {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 191 | <code>  cursor: zoom-in;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 192 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 193 | <code>.post-video-grid {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 194 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 195 | <code>  gap:5px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 196 | <code>  margin-top:16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 197 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 198 | <code>.post-video-grid.one { grid-template-columns:1fr; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 199 | <code>.post-video-grid.two { grid-template-columns:repeat(2,1fr); }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 200 | <code>.post-video-grid.many { grid-template-columns:repeat(3,1fr); }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 201 | <code>.video-attachment-item {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 202 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 203 | <code>  min-width:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 204 | <code>  overflow:hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 205 | <code>  border-radius:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 206 | <code>  background:#111;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 207 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 208 | <code>.post-video-grid .video-attachment-item { aspect-ratio:16/9; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 209 | <code>.post-video-grid.two .video-attachment-item,</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 210 | <code>.post-video-grid.many .video-attachment-item { aspect-ratio:1/1; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 211 | <code>.video-attachment-item video {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 212 | <code>  display:block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 213 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 214 | <code>  height:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 215 | <code>  object-fit:contain;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 216 | <code>  background:#000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 217 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 218 | <code>.video-expand {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 219 | <code>  position:absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 220 | <code>  top:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 221 | <code>  right:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 222 | <code>  width:34px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 223 | <code>  height:34px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 224 | <code>  padding:7px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 225 | <code>  border:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 226 | <code>  border-radius:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 227 | <code>  background:rgba(0,0,0,.58);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 228 | <code>  color:#fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 229 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 230 | <code>  place-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 231 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 232 | <code>.video-expand svg { width:20px; height:20px; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 233 | <code>.compose-video-item .video-expand { left:8px; right:auto; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 234 | <code>.video-unavailable {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 235 | <code>  padding:18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 236 | <code>  color:#fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 237 | <code>  background:#333;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 238 | <code>  font-size:13px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 239 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 240 | <code>.compose-video-preview {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 241 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 242 | <code>  grid-template-columns:repeat(3,1fr);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 243 | <code>  gap:7px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 244 | <code>  margin-top:16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 245 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 246 | <code>.compose-video-preview:empty { display:none; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 247 | <code>.compose-video-item {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 248 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 249 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 250 | <code>  aspect-ratio:1/1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 251 | <code>  overflow:hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 252 | <code>  border:1px solid #dedede;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 253 | <code>  border-radius:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 254 | <code>  background:#111;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 255 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 256 | <code>.compose-video-item video {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 257 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 258 | <code>  height:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 259 | <code>  object-fit:contain;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 260 | <code>  background:#000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 261 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 262 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 263 | <code>.image-viewer {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 264 | <code>  position: fixed;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 265 | <code>  inset: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 266 | <code>  z-index: 30000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 267 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 268 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 269 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 270 | <code>  background: rgba(0,0,0,.96);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 271 | <code>  overflow: hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 272 | <code>  touch-action: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 273 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 274 | <code>.image-viewer.open {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 275 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 276 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 277 | <code>.image-viewer img,</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 278 | <code>.image-viewer video {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 279 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 280 | <code>  max-width: calc(100vw - 24px);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 281 | <code>  max-height: calc(100dvh - 24px);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 282 | <code>  width: auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 283 | <code>  height: auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 284 | <code>  object-fit: contain;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 285 | <code>  transform-origin: center center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 286 | <code>  transition: transform .14s ease-out;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 287 | <code>  touch-action: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 288 | <code>  user-select: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 289 | <code>  -webkit-user-drag: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 290 | <code>  cursor: zoom-in;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 291 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 292 | <code>.image-viewer img,</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 293 | <code>.image-viewer video { display:none; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 294 | <code>.image-viewer img.active,</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 295 | <code>.image-viewer video.active { display:block; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 296 | <code>.image-viewer video {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 297 | <code>  background:#000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 298 | <code>  /* Keep Android/WebView's native media controls responsive. */</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 299 | <code>  touch-action: manipulation;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 300 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 301 | <code>.image-viewer img.zoomed,</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 302 | <code>.image-viewer video.zoomed {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 303 | <code>  cursor: grab;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 304 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 305 | <code>.image-viewer-close {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 306 | <code>  position: fixed;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 307 | <code>  right: 18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 308 | <code>  top: 18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 309 | <code>  z-index: 2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 310 | <code>  width: 52px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 311 | <code>  height: 52px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 312 | <code>  padding: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 313 | <code>  border: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 314 | <code>  background: transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 315 | <code>  color: transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 316 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 317 | <code>  mix-blend-mode: difference;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 318 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 319 | <code>.image-viewer-close::before {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 320 | <code>  content: "";</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 321 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 322 | <code>  left: 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 323 | <code>  top: 24px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 324 | <code>  width: 34px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 325 | <code>  height: 4px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 326 | <code>  border-radius: 2px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 327 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 328 | <code>  transform-origin: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 329 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 330 | <code>.image-viewer-close::after {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 331 | <code>  content: "";</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 332 | <code>  position: absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 333 | <code>  left: 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 334 | <code>  top: 24px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 335 | <code>  width: 34px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 336 | <code>  height: 4px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 337 | <code>  border-radius: 2px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 338 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 339 | <code>  transform-origin: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 340 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 341 | <code>.image-viewer-close::before {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 342 | <code>  transform: rotate(45deg);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 343 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 344 | <code>.image-viewer-close::after {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 345 | <code>  transform: rotate(-45deg);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 346 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 347 | <code>.post-compose-images .compose-image-item img {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 348 | <code>  cursor: zoom-in;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 349 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 350 | <code>.delete-confirm-backdrop {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 351 | <code>  position: fixed;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 352 | <code>  inset: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 353 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 354 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 355 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 356 | <code>  padding: 24px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 357 | <code>  background: rgba(0,0,0,.18);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 358 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 359 | <code>.delete-confirm-backdrop.open {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 360 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 361 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 362 | <code>.delete-confirm-btn:active {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 363 | <code>  background: #f5f5f5;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 364 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 365 | <code>.delete-confirm-btn svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 366 | <code>  display: block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 367 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 368 | <code>.delete-confirm-btn.delete {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 369 | <code>  color: #d93636;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 370 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 371 | <code>.delete-confirm-btn.delete svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 372 | <code>  stroke: #d93636;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 373 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 374 | <code>.post-action-panel {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 375 | <code>  position: fixed !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 376 | <code>  left: 50% !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 377 | <code>  top: 50% !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 378 | <code>  transform: translate(-50%, -50%) !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 379 | <code>  z-index: 30020 !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 380 | <code>  width: 286px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 381 | <code>  height: auto !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 382 | <code>  display: none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 383 | <code>  grid-template-columns: 1fr 1fr;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 384 | <code>  gap: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 385 | <code>  padding: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 386 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 387 | <code>  border: 1px solid rgb(155,155,155);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 388 | <code>  border-radius: 15px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 389 | <code>  box-shadow: 0 8px 24px rgba(0,0,0,.12);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 390 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 391 | <code>.post-action-panel.open {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 392 | <code>  display: grid !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 393 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 394 | <code>.post-action-btn {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 395 | <code>  min-height: 126px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 396 | <code>  height: 126px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 397 | <code>  border: 1px solid rgb(155,155,155);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 398 | <code>  border-radius: 13px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 399 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 400 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 401 | <code>  flex-direction: column;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 402 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 403 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 404 | <code>  gap: 8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 405 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 406 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 407 | <code>.post-action-icon {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 408 | <code>  width: 32px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 409 | <code>  height: 32px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 410 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 411 | <code>.post-action-label {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 412 | <code>  font-size: 18px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 413 | <code>  line-height: 1.2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 414 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 415 | <code>.delete-confirm-backdrop {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 416 | <code>  z-index: 30010 !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 417 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 418 | <code>.delete-confirm-panel {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 419 | <code>  width: 286px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 420 | <code>  padding: 10px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 421 | <code>  border: 1px solid rgb(155,155,155);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 422 | <code>  border-radius: 15px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 423 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 424 | <code>  box-shadow: 0 8px 24px rgba(0,0,0,.12);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 425 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 426 | <code>.delete-confirm-message {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 427 | <code>  padding: 10px 8px 12px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 428 | <code>  text-align: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 429 | <code>  color: #222;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 430 | <code>  font-size: 16px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 431 | <code>  line-height: 1.35;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 432 | <code>  font-weight: 600;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 433 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 434 | <code>.delete-confirm-actions {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 435 | <code>  display: grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 436 | <code>  grid-template-columns: 1fr 1fr;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 437 | <code>  gap: 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 438 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 439 | <code>.delete-confirm-btn {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 440 | <code>  min-height: 126px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 441 | <code>  height: 126px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 442 | <code>  border: 1px solid rgb(155,155,155);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 443 | <code>  border-radius: 13px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 444 | <code>  background: #fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 445 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 446 | <code>  flex-direction: column;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 447 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 448 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 449 | <code>  gap: 8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 450 | <code>  color: #222;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 451 | <code>  font-size: 18px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 452 | <code>  font-weight: 500;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 453 | <code>  cursor: pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 454 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 455 | <code>.delete-confirm-btn svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 456 | <code>  width: 32px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 457 | <code>  height: 32px !important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 458 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 459 | <code>.jet-notice-backdrop{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 460 | <code>  display:none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 461 | <code>  position:fixed;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 462 | <code>  inset:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 463 | <code>  z-index:60000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 464 | <code>  align-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 465 | <code>  justify-content:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 466 | <code>  padding:20px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 467 | <code>  background:rgba(20,27,35,.32);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 468 | <code>  backdrop-filter:blur(3px)</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 469 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 470 | <code>.jet-notice-backdrop.open{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 471 | <code>  display:flex</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 472 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 473 | <code>.jet-notice{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 474 | <code>  width:min(310px,100%);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 475 | <code>  padding:20px 18px 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 476 | <code>  border:1px solid rgba(70,84,99,.2);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 477 | <code>  border-radius:18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 478 | <code>  background:#fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 479 | <code>  box-shadow:0 18px 46px rgba(24,32,42,.22);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 480 | <code>  text-align:center</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 481 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 482 | <code>.jet-notice-mark{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 483 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 484 | <code>  place-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 485 | <code>  width:34px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 486 | <code>  height:34px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 487 | <code>  margin:0 auto 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 488 | <code>  border-radius:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 489 | <code>  background:#eaf4fb;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 490 | <code>  color:#315f98;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 491 | <code>  font-weight:700</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 492 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 493 | <code>.jet-notice h2{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 494 | <code>  margin:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 495 | <code>  font-size:18px</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 496 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 497 | <code>.jet-notice p{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 498 | <code>  margin:10px 0 18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 499 | <code>  white-space:pre-wrap;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 500 | <code>  overflow-wrap:anywhere;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 501 | <code>  color:#4d545c;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 502 | <code>  line-height:1.5</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 503 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 504 | <code>.jet-notice button{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 505 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 506 | <code>  min-height:44px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 507 | <code>  border:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 508 | <code>  border-radius:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 509 | <code>  background:#315f98;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 510 | <code>  color:#fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 511 | <code>  font-weight:600</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 512 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 513 | <code>/* Post menus share one compact shape and are positioned left of the tapped dots. */</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 514 | <code>.post-action-panel{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 515 | <code>  position:fixed!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 516 | <code>  left:auto!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 517 | <code>  top:auto!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 518 | <code>  right:auto!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 519 | <code>  transform:none!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 520 | <code>  z-index:32020!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 521 | <code>  width:max-content!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 522 | <code>  max-width:calc(100vw - 16px)!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 523 | <code>  display:none!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 524 | <code>  grid-template-columns:none!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 525 | <code>  gap:2px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 526 | <code>  padding:4px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 527 | <code>  border:1px solid #bfc1c4!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 528 | <code>  border-radius:11px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 529 | <code>  background:#fff!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 530 | <code>  box-shadow:0 5px 18px rgba(21,31,42,.18)!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 531 | <code>  pointer-events:auto!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 532 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 533 | <code>.post-action-panel.open{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 534 | <code>  display:flex!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 535 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 536 | <code>.post-action-btn{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 537 | <code>  width:auto!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 538 | <code>  height:36px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 539 | <code>  min-height:36px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 540 | <code>  padding:6px 10px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 541 | <code>  border:0!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 542 | <code>  border-radius:8px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 543 | <code>  background:#fff!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 544 | <code>  display:flex!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 545 | <code>  flex-direction:row!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 546 | <code>  align-items:center!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 547 | <code>  justify-content:center!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 548 | <code>  gap:5px!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 549 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 550 | <code>.post-action-icon{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 551 | <code>  width:17px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 552 | <code>  height:17px!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 553 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 554 | <code>.post-action-label{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 555 | <code>  font-size:14px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 556 | <code>  white-space:nowrap</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 557 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 558 | <code>.more{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 559 | <code>  width:32px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 560 | <code>  height:32px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 561 | <code>  padding:6px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 562 | <code>  top:4px!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 563 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 564 | <code>.more svg{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 565 | <code>  width:19px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 566 | <code>  height:19px!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 567 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 568 | <code>.delete-confirm-backdrop.post-menu-confirm{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 569 | <code>  display:none!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 570 | <code>  position:fixed!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 571 | <code>  inset:0!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 572 | <code>  z-index:32030!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 573 | <code>  padding:0!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 574 | <code>  background:transparent!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 575 | <code>  align-items:initial!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 576 | <code>  justify-content:initial!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 577 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 578 | <code>.delete-confirm-backdrop.post-menu-confirm.open{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 579 | <code>  display:block!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 580 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 581 | <code>.delete-confirm-backdrop.post-menu-confirm .delete-confirm-panel{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 582 | <code>  position:fixed!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 583 | <code>  left:auto!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 584 | <code>  top:auto!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 585 | <code>  right:auto!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 586 | <code>  transform:none!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 587 | <code>  width:max-content!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 588 | <code>  max-width:calc(100vw - 16px)!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 589 | <code>  padding:4px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 590 | <code>  border:1px solid #bfc1c4!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 591 | <code>  border-radius:11px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 592 | <code>  background:#fff!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 593 | <code>  box-shadow:0 5px 18px rgba(21,31,42,.18)!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 594 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 595 | <code>.delete-confirm-backdrop.post-menu-confirm .delete-confirm-message{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 596 | <code>  position:absolute!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 597 | <code>  width:1px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 598 | <code>  height:1px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 599 | <code>  padding:0!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 600 | <code>  margin:-1px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 601 | <code>  overflow:hidden!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 602 | <code>  clip:rect(0 0 0 0)!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 603 | <code>  white-space:nowrap!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 604 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 605 | <code>.delete-confirm-backdrop.post-menu-confirm .delete-confirm-actions{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 606 | <code>  display:flex!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 607 | <code>  grid-template-columns:none!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 608 | <code>  gap:2px!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 609 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 610 | <code>.delete-confirm-backdrop.post-menu-confirm .delete-confirm-btn{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 611 | <code>  width:auto!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 612 | <code>  height:36px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 613 | <code>  min-height:36px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 614 | <code>  padding:6px 10px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 615 | <code>  border:0!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 616 | <code>  border-radius:8px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 617 | <code>  background:#fff!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 618 | <code>  display:flex!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 619 | <code>  flex-direction:row!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 620 | <code>  align-items:center!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 621 | <code>  justify-content:center!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 622 | <code>  gap:5px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 623 | <code>  color:#222!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 624 | <code>  font-size:14px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 625 | <code>  font-weight:400!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 626 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 627 | <code>.delete-confirm-backdrop.post-menu-confirm .delete-confirm-btn.delete{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 628 | <code>  color:#d93636!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 629 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 630 | <code>.delete-confirm-backdrop.post-menu-confirm .delete-confirm-btn svg{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 631 | <code>  width:17px!important;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 632 | <code>  height:17px!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 633 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 634 | <code>.delete-confirm-backdrop.post-menu-confirm .delete-confirm-btn span{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 635 | <code>  white-space:nowrap!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 636 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 637 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 638 | <code>/* V39 — stable video cards. WebView renders only a poster; Android owns playback. */</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 639 | <code>.native-video-card {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 640 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 641 | <code>  background:#111;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 642 | <code>  cursor:pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 643 | <code>  user-select:none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 644 | <code>  -webkit-user-select:none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 645 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 646 | <code>.native-video-card .video-poster {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 647 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 648 | <code>  z-index:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 649 | <code>  display:block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 650 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 651 | <code>  height:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 652 | <code>  object-fit:cover;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 653 | <code>  background:linear-gradient(145deg,#333,#111);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 654 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 655 | <code>.native-video-card.poster-unavailable .video-poster { opacity:0; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 656 | <code>.video-play {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 657 | <code>  z-index:3;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 658 | <code>  position:absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 659 | <code>  left:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 660 | <code>  top:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 661 | <code>  transform:translate(-50%,-50%);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 662 | <code>  width:58px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 663 | <code>  height:58px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 664 | <code>  border:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 665 | <code>  border-radius:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 666 | <code>  background:rgba(255,255,255,.9);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 667 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 668 | <code>  place-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 669 | <code>  padding:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 670 | <code>  box-shadow:0 2px 12px rgba(0,0,0,.24);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 671 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 672 | <code>.video-play-triangle {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 673 | <code>  display:block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 674 | <code>  width:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 675 | <code>  height:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 676 | <code>  border-top:10px solid transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 677 | <code>  border-bottom:10px solid transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 678 | <code>  border-left:16px solid #111;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 679 | <code>  margin-left:4px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 680 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 681 | <code>.compose-video-item .video-play {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 682 | <code>  width:44px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 683 | <code>  height:44px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 684 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 685 | <code>.compose-video-item .video-play-triangle {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 686 | <code>  border-top-width:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 687 | <code>  border-bottom-width:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 688 | <code>  border-left-width:13px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 689 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 690 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 691 | <code>/* V40 — lazy inline video player. No Chromium native controls are used. */</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 692 | <code>.native-video-card .inline-video-player {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 693 | <code>  position:absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 694 | <code>  inset:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 695 | <code>  z-index:1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 696 | <code>  display:block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 697 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 698 | <code>  height:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 699 | <code>  object-fit:contain;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 700 | <code>  background:#000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 701 | <code>  transform:translateZ(0);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 702 | <code>  -webkit-transform:translateZ(0);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 703 | <code>  backface-visibility:hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 704 | <code>  -webkit-backface-visibility:hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 705 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 706 | <code>.native-video-card.video-loaded { cursor:default; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 707 | <code>.video-inline-controls {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 708 | <code>  position:absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 709 | <code>  inset:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 710 | <code>  z-index:2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 711 | <code>  pointer-events:none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 712 | <code>  opacity:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 713 | <code>  visibility:hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 714 | <code>  transition:opacity .14s ease, visibility .14s ease;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 715 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 716 | <code>.native-video-card.video-controls-visible .video-inline-controls {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 717 | <code>  opacity:1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 718 | <code>  visibility:visible;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 719 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 720 | <code>.video-inline-toggle {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 721 | <code>  pointer-events:auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 722 | <code>  position:absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 723 | <code>  left:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 724 | <code>  top:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 725 | <code>  transform:translate(-50%,-50%);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 726 | <code>  width:58px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 727 | <code>  height:58px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 728 | <code>  border:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 729 | <code>  border-radius:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 730 | <code>  padding:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 731 | <code>  background:rgba(255,255,255,.88);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 732 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 733 | <code>  place-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 734 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 735 | <code>.video-pause-symbol {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 736 | <code>  width:18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 737 | <code>  height:23px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 738 | <code>  border-left:6px solid #111;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 739 | <code>  border-right:6px solid #111;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 740 | <code>  box-sizing:border-box;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 741 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 742 | <code>.video-inline-bottom {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 743 | <code>  pointer-events:auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 744 | <code>  position:absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 745 | <code>  left:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 746 | <code>  right:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 747 | <code>  bottom:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 748 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 749 | <code>  align-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 750 | <code>  gap:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 751 | <code>  padding:7px 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 752 | <code>  border-radius:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 753 | <code>  background:rgba(0,0,0,.5);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 754 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 755 | <code>.video-inline-time {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 756 | <code>  flex:0 0 auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 757 | <code>  color:#fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 758 | <code>  font-size:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 759 | <code>  font-variant-numeric:tabular-nums;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 760 | <code>  white-space:nowrap;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 761 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 762 | <code>.video-inline-seek {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 763 | <code>  flex:1 1 auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 764 | <code>  min-width:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 765 | <code>  margin:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 766 | <code>  accent-color:#fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 767 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 768 | <code>.compose-video-item .video-inline-toggle {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 769 | <code>  width:42px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 770 | <code>  height:42px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 771 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 772 | <code>.compose-video-item .video-inline-bottom {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 773 | <code>  left:6px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 774 | <code>  right:6px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 775 | <code>  bottom:5px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 776 | <code>  gap:5px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 777 | <code>  padding:4px 5px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 778 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 779 | <code>.compose-video-item .video-inline-time { display:none; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 780 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 781 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 782 | <code>/* First-boot demo loader */</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 783 | <code>.startup-loading {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 784 | <code>  position: fixed;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 785 | <code>  inset: 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 786 | <code>  z-index: 100000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 787 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 788 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 789 | <code>  justify-content: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 790 | <code>  padding: 28px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 791 | <code>  background: rgb(255,255,255);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 792 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 793 | <code>.startup-loading[hidden] { display: none !important; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 794 | <code>.startup-loading-panel {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 795 | <code>  width: min(360px, 84vw);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 796 | <code>  text-align: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 797 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 798 | <code>.startup-loading-title {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 799 | <code>  margin-bottom: 18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 800 | <code>  color: #35a65a;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 801 | <code>  font-size: 20px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 802 | <code>  font-weight: 650;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 803 | <code>  letter-spacing: .04em;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 804 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 805 | <code>.startup-loading-track {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 806 | <code>  height: 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 807 | <code>  overflow: hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 808 | <code>  border: 1px solid #cfd8d2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 809 | <code>  border-radius: 999px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 810 | <code>  background: #f4f7f5;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 811 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 812 | <code>.startup-loading-fill {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 813 | <code>  width: 0%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 814 | <code>  height: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 815 | <code>  border-radius: inherit;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 816 | <code>  background: #35a65a;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 817 | <code>  transition: width .16s linear;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 818 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 819 | <code>.startup-loading-percent {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 820 | <code>  margin-top: 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 821 | <code>  color: #7d8781;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 822 | <code>  font-size: 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 823 | <code>  font-variant-numeric: tabular-nums;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 824 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 825 | <code>html.startup-loading-active body { overflow: hidden !important; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
