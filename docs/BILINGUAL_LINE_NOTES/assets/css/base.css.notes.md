# assets/css/base.css — 双语逐行备注 / Bilingual line notes

原文件保持不变，以下备注按原文件行号对应。JSON 和第三方压缩库不插入行内注释，以避免破坏解析或依赖完整性。

The source file remains unchanged. These notes map to its original line numbers. JSON and minified third-party libraries receive sidecar notes so parsing and dependency integrity stay intact.

| 行 / Line | 原始内容 / Source | 中文备注 | English note |
| ---: | --- | --- | --- |
| 1 | <code>:root {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 2 | <code>  color-scheme: light;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 3 | <code>  --ink:#111214;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 4 | <code>  --muted:#999da2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 5 | <code>  --blue:#315f98;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 6 | <code>  --page-background:rgb(255,255,255);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 7 | <code>  --page-background-image:none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 8 | <code>  --feed:var(--page-background);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 9 | <code>  --body-size:17px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 10 | <code>  --body-leading:1.5;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 11 | <code>  --card-radius:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 12 | <code>  --safe-top:env(safe-area-inset-top,0px);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 13 | <code>  --safe-bottom:env(safe-area-inset-bottom,0px);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 14 | <code>  --safe-left:env(safe-area-inset-left,0px);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 15 | <code>  --safe-right:env(safe-area-inset-right,0px);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 16 | <code>  /* Matches the native Dictionary / Sentence toolbar: 50dp bar, 36dp controls. */</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 17 | <code>  --jet-toolbar-height:50px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 18 | <code>  --jet-toolbar-control-height:36px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 19 | <code>  --jet-toolbar-title-size:16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 20 | <code>  --jet-toolbar-control-size:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 21 | <code>  --jet-toolbar-control-padding-x:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 22 | <code>  --jet-toolbar-control-radius:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 23 | <code>  --jet-toolbar-side-slot:88px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 24 | <code>  --jet-toolbar-horizontal-padding:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 25 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 26 | <code>html,body {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 27 | <code>  margin:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 28 | <code>  min-height:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 29 | <code>  color:var(--ink);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 30 | <code>  font-family:-apple-system,BlinkMacSystemFont,'PingFang SC','Microsoft YaHei',Arial,sans-serif;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 31 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 32 | <code>* {</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 33 | <code>  box-sizing:border-box;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 34 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 35 | <code>body {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 36 | <code>  background-color:var(--page-background);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 37 | <code>  background-image:var(--page-background-image);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 38 | <code>  background-size:cover;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 39 | <code>  background-position:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 40 | <code>  background-attachment:fixed;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 41 | <code>  overflow-x:hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 42 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 43 | <code>button,input,textarea {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 44 | <code>  font:inherit;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 45 | <code>  color:inherit;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 46 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 47 | <code>button {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 48 | <code>  cursor:pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 49 | <code>  -webkit-tap-highlight-color:transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 50 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 51 | <code>button:focus-visible {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 52 | <code>  outline:2px solid #1599e8;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 53 | <code>  outline-offset:2px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 54 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 55 | <code>#stage {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 56 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 57 | <code>  margin:auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 58 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 59 | <code>.phone {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 60 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 61 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 62 | <code>  min-height:100vh;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 63 | <code>  min-height:100dvh;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 64 | <code>  padding:calc(var(--safe-top) + 14px) var(--safe-right) calc(var(--safe-bottom) + 16px) var(--safe-left);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 65 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 66 | <code>.header,.ghost-line,.wave3,.composer-area {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 67 | <code>  display:none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 68 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 69 | <code>.profile {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 70 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 71 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 72 | <code>  align-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 73 | <code>  gap:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 74 | <code>  margin:0 10px 8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 75 | <code>  padding:12px 48px 12px 14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 76 | <code>  min-height:92px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 77 | <code>  border-radius:22px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 78 | <code>  background:linear-gradient(#ffffffeb,#eff7fbe6);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 79 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 80 | <code>.avatar {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 81 | <code>  display:block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 82 | <code>  width:70px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 83 | <code>  height:70px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 84 | <code>  flex-shrink:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 85 | <code>  border-radius:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 86 | <code>  object-fit:cover;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 87 | <code>  cursor:pointer;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 88 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 89 | <code>.name {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 90 | <code>  min-width:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 91 | <code>  font-size:clamp(19px,5.2vw,26px);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 92 | <code>  font-weight:500;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 93 | <code>  overflow-wrap:anywhere;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 94 | <code>  outline:none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 95 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 96 | <code>.settings {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 97 | <code>  position:absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 98 | <code>  top:9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 99 | <code>  right:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 100 | <code>  border:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 101 | <code>  border-radius:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 102 | <code>  width:36px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 103 | <code>  height:36px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 104 | <code>  padding:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 105 | <code>  background:#a7b0ba66;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 106 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 107 | <code>  place-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 108 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 109 | <code>.settings svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 110 | <code>  width:22px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 111 | <code>  height:22px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 112 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 113 | <code>.tabs {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 114 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 115 | <code>  margin:0 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 116 | <code>  border-radius:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 117 | <code>  overflow:visible;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 118 | <code>  background:transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 119 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 120 | <code>.tab {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 121 | <code>  flex:1 1 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 122 | <code>  min-width:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 123 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 124 | <code>  flex-direction:column;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 125 | <code>  align-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 126 | <code>  justify-content:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 127 | <code>  min-height:76px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 128 | <code>  border:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 129 | <code>  background:transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 130 | <code>  padding:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 131 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 132 | <code>.tab svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 133 | <code>  width:30px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 134 | <code>  height:30px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 135 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 136 | <code>.tab-label {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 137 | <code>  margin-top:6px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 138 | <code>  font-size:16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 139 | <code>  line-height:1.1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 140 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 141 | <code>.feed {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 142 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 143 | <code>  padding:0 8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 144 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 145 | <code>.feed-composer-wrap {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 146 | <code>  margin:0 -2px 8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 147 | <code>  padding:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 148 | <code>  border-radius:0 0 18px 18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 149 | <code>  background:#ffffffeb;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 150 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 151 | <code>.feed-composer {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 152 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 153 | <code>  align-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 154 | <code>  min-height:54px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 155 | <code>  border:1px solid #cfd1d5;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 156 | <code>  border-radius:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 157 | <code>  background:white;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 158 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 159 | <code>.feed-composer-main {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 160 | <code>  flex:1;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 161 | <code>  min-width:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 162 | <code>  min-height:52px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 163 | <code>  border:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 164 | <code>  background:transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 165 | <code>  text-align:left;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 166 | <code>  padding:10px 14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 167 | <code>  color:#9da0a4;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 168 | <code>  font-size:16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 169 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 170 | <code>.feed-composer-photo {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 171 | <code>  width:48px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 172 | <code>  height:48px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 173 | <code>  border:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 174 | <code>  background:transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 175 | <code>  padding:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 176 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 177 | <code>.feed-composer-photo svg {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 178 | <code>  width:26px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 179 | <code>  height:26px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 180 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 181 | <code>.post {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 182 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 183 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 184 | <code>  margin:0 0 8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 185 | <code>  padding:12px 12px 14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 186 | <code>  border:1px solid #bfc1c4;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 187 | <code>  border-radius:var(--card-radius);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 188 | <code>  background:white;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 189 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 190 | <code>.post-head {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 191 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 192 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 193 | <code>  align-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 194 | <code>  gap:6px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 195 | <code>  min-height:42px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 196 | <code>  padding-right:44px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 197 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 198 | <code>.mini-avatar {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 199 | <code>  width:38px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 200 | <code>  height:38px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 201 | <code>  border-radius:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 202 | <code>  object-fit:cover;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 203 | <code>  flex-shrink:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 204 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 205 | <code>.post-name {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 206 | <code>  color:var(--blue);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 207 | <code>  font-size:16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 208 | <code>  font-weight:600;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 209 | <code>  overflow-wrap:anywhere;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 210 | <code>  min-width:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 211 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 212 | <code>.text-content {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 213 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 214 | <code>  margin-top:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 215 | <code>  white-space:pre-wrap;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 216 | <code>  overflow-wrap:anywhere;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 217 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 218 | <code>.time {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 219 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 220 | <code>  margin-top:16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 221 | <code>  color:var(--muted);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 222 | <code>  font-size:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 223 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 224 | <code>@media(min-width:720px) {</code> | 定义 CSS 的全局规则、媒体条件或关键帧。 | Defines a global CSS rule, media condition, or keyframe. |
| 225 | <code>  .profile,.tabs,.feed {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 226 | <code>    max-width:700px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 227 | <code>    margin-left:auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 228 | <code>    margin-right:auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 229 | <code>  }</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 230 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 231 | <code>/* Keep settings near the top while moving the avatar/name down. The 8px</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 232 | <code>   profile bottom padding plus the tab's 10px top padding keeps the avatar</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 233 | <code>   bottom about 18px from the feature icon row, safely below the 60px limit. */</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 234 | <code>.profile{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 235 | <code>  background:transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 236 | <code>  border-radius:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 237 | <code>  padding:36px 44px 8px 4px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 238 | <code>  min-height:114px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 239 | <code>  margin-bottom:0</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 240 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 241 | <code>.tabs{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 242 | <code>  background:transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 243 | <code>  border-radius:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 244 | <code>  overflow:visible</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 245 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 246 | <code>.feed-composer-wrap{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 247 | <code>  background:transparent;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 248 | <code>  border-radius:0</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 249 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 250 | <code>.settings{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 251 | <code>  background:rgba(255,255,255,.48);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 252 | <code>  color:#48515b</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 253 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 254 | <code>.settings path{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 255 | <code>  fill:currentColor!important</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 256 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 257 | <code>:root[data-avatar-shape="round"] .avatar,:root[data-avatar-shape="round"] .mini-avatar{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 258 | <code>  border-radius:50%</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 259 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 260 | <code>:root[data-avatar-shape="square"] .avatar{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 261 | <code>  border-radius:12px</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 262 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 263 | <code>:root[data-avatar-shape="square"] .mini-avatar{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 264 | <code>  border-radius:7px</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 265 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
