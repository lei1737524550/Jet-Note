# assets/css/attachments.css — 双语逐行备注 / Bilingual line notes

原文件保持不变，以下备注按原文件行号对应。JSON 和第三方压缩库不插入行内注释，以避免破坏解析或依赖完整性。

The source file remains unchanged. These notes map to its original line numbers. JSON and minified third-party libraries receive sidecar notes so parsing and dependency integrity stay intact.

| 行 / Line | 原始内容 / Source | 中文备注 | English note |
| ---: | --- | --- | --- |
| 1 | <code>.tabs {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 2 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 3 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 4 | <code>.tabs .tab {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 5 | <code>  flex:1 1 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 6 | <code>  min-width:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 7 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 8 | <code>.tab-label {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 9 | <code>  font-size:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 10 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 11 | <code>.attachment-list {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 12 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 13 | <code>  gap:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 14 | <code>  margin:12px 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 15 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 16 | <code>.attachment-item {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 17 | <code>  min-width:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 18 | <code>  border:1px solid #c8cdd2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 19 | <code>  border-radius:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 20 | <code>  background:#f8fafb;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 21 | <code>  padding:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 22 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 23 | <code>.attachment-name {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 24 | <code>  font-size:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 25 | <code>  overflow-wrap:anywhere;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 26 | <code>  margin-bottom:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 27 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 28 | <code>.attachment-item audio {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 29 | <code>  display:block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 30 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 31 | <code>  height:44px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 32 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 33 | <code>.attachment-item button {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 34 | <code>  margin-top:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 35 | <code>  padding:6px 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 36 | <code>  border:1px solid #ddd;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 37 | <code>  border-radius:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 38 | <code>  background:white;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 39 | <code>  font-size:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 40 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 41 | <code>.attachment-item small {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 42 | <code>  display:block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 43 | <code>  margin-top:6px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 44 | <code>  font-size:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 45 | <code>  color:#925000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 46 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 47 | <code>.compose-media-toolbar {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 48 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 49 | <code>  flex-wrap:wrap;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 50 | <code>  gap:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 51 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 52 | <code>.compose-add-image {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 53 | <code>  min-height:44px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 54 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 55 | <code>.import-panel button {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 56 | <code>  min-height:44px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 57 | <code>  padding:10px 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 58 | <code>  border:1px solid #bfc1c4;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 59 | <code>  border-radius:12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 60 | <code>  background:white;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 61 | <code>  margin:4px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 62 | <code>  font-size:15px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 63 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 64 | <code>#archiveStatus {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 65 | <code>  font-size:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 66 | <code>  overflow-wrap:anywhere;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 67 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 68 | <code>.import-preview {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 69 | <code>  display:none;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 70 | <code>  position:fixed;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 71 | <code>  inset:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 72 | <code>  z-index:45000;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 73 | <code>  background:#0006;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 74 | <code>  align-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 75 | <code>  justify-content:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 76 | <code>  padding:16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 77 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 78 | <code>.import-preview.open {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 79 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 80 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 81 | <code>.import-panel {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 82 | <code>  width:min(440px,100%);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 83 | <code>  max-height:calc(100dvh - 60px);</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 84 | <code>  overflow:auto;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 85 | <code>  padding:18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 86 | <code>  background:white;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 87 | <code>  border:1px solid #bfc1c4;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 88 | <code>  border-radius:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 89 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 90 | <code>.import-panel select {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 91 | <code>  display:block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 92 | <code>  width:100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 93 | <code>  min-height:44px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 94 | <code>  margin:12px 0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 95 | <code>  font:inherit;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 96 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 97 | <code>.import-panel p {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 98 | <code>  font-size:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 99 | <code>  line-height:1.5;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 100 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 101 | <code>button:disabled {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 102 | <code>  opacity:.45;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 103 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 104 | <code>/* Compact post audio: one source icon per independent attachment. */</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 105 | <code>.attachment-strip{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 106 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 107 | <code>  justify-content:flex-end;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 108 | <code>  align-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 109 | <code>  flex-wrap:wrap;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 110 | <code>  gap:6px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 111 | <code>  margin:9px 2px 4px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 112 | <code>  min-height:34px</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 113 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 114 | <code>.attachment-strip .audio-token{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 115 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 116 | <code>  display:inline-flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 117 | <code>  align-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 118 | <code>  padding:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 119 | <code>  border:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 120 | <code>  border-radius:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 121 | <code>  background:transparent</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 122 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 123 | <code>.audio-token audio{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 124 | <code>  display:none</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 125 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 126 | <code>.audio-token-play{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 127 | <code>  position:relative;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 128 | <code>  width:34px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 129 | <code>  height:34px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 130 | <code>  padding:7px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 131 | <code>  border:1px solid #cbd2d9;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 132 | <code>  border-radius:11px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 133 | <code>  background:#f8fafc;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 134 | <code>  color:#315f98;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 135 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 136 | <code>  place-items:center</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 137 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 138 | <code>.audio-token-play svg{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 139 | <code>  width:19px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 140 | <code>  height:19px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 141 | <code>  transform:translateX(-4px)</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 142 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 143 | <code>.audio-token-play sub{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 144 | <code>  position:absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 145 | <code>  right:-3px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 146 | <code>  bottom:-3px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 147 | <code>  min-width:15px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 148 | <code>  height:15px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 149 | <code>  padding:0 3px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 150 | <code>  border-radius:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 151 | <code>  background:#315f98;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 152 | <code>  color:#fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 153 | <code>  font-size:9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 154 | <code>  line-height:15px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 155 | <code>  text-align:center</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 156 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 157 | <code>.audio-token-play.playing{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 158 | <code>  background:#315f98;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 159 | <code>  color:#fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 160 | <code>  box-shadow:0 0 0 3px rgba(49,95,152,.14)</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 161 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 162 | <code>.audio-token-play.unavailable{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 163 | <code>  opacity:.45</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 164 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 165 | <code>.audio-token-remove{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 166 | <code>  position:absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 167 | <code>  right:-7px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 168 | <code>  top:-8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 169 | <code>  width:18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 170 | <code>  height:18px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 171 | <code>  padding:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 172 | <code>  border:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 173 | <code>  border-radius:50%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 174 | <code>  background:#d8dde2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 175 | <code>  color:#333;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 176 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 177 | <code>  align-items:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 178 | <code>  justify-content:center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 179 | <code>  box-sizing:border-box</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 180 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 181 | <code>.audio-token-remove svg{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 182 | <code>  display:block;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 183 | <code>  width:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 184 | <code>  height:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 185 | <code>  flex:0 0 10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 186 | <code>  overflow:visible;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 187 | <code>  stroke:currentColor;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 188 | <code>  stroke-width:3;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 189 | <code>  stroke-linecap:round;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 190 | <code>  fill:none</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 191 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 192 | <code>.attachment-unknown{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 193 | <code>  padding:6px 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 194 | <code>  border:1px solid #d7dce0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 195 | <code>  border-radius:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 196 | <code>  background:#fafbfc;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 197 | <code>  font-size:11px</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 198 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 199 | <code>.post-input-row{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 200 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 201 | <code>  grid-template-columns:minmax(0,1fr) 54px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 202 | <code>  gap:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 203 | <code>  align-items:start</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 204 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 205 | <code>.post-input-row #postComposerText{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 206 | <code>  min-width:0</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 207 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 208 | <code>.post-input-row .compose-media-toolbar{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 209 | <code>  display:flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 210 | <code>  flex-direction:column;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 211 | <code>  justify-content:flex-start;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 212 | <code>  gap:8px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 213 | <code>  margin:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 214 | <code>  padding:0;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 215 | <code>  border:0</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 216 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 217 | <code>.post-input-row .compose-add-image{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 218 | <code>  width:48px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 219 | <code>  height:48px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 220 | <code>  min-height:48px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 221 | <code>  padding:10px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 222 | <code>  border:1px solid #d4d9de;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 223 | <code>  border-radius:14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 224 | <code>  background:#fff;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 225 | <code>  display:grid;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 226 | <code>  place-items:center</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 227 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 228 | <code>.post-input-row .compose-add-image svg{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 229 | <code>  width:24px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 230 | <code>  height:24px</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 231 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 232 | <code>.post-input-row .compose-add-image span{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 233 | <code>  position:absolute;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 234 | <code>  width:1px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 235 | <code>  height:1px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 236 | <code>  overflow:hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 237 | <code>  clip-path:inset(50%)</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 238 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 239 | <code>#postAudioDraft .attachment-strip{</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 240 | <code>  justify-content:flex-end;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 241 | <code>  margin:10px 0 4px</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 242 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 243 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 244 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 245 | <code>/* Transactional .jnote transfer progress */</code> | 原始注释：为开发者保留背景、约束或实现说明。 | Existing comment: preserves context, constraints, or implementation guidance for developers. |
| 246 | <code>.archive-progress {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 247 | <code>  margin: 4px 14px 14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 248 | <code>  padding: 12px 13px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 249 | <code>  border: 1px solid #d8dde2;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 250 | <code>  border-radius: 14px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 251 | <code>  background: #fbfcfd;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 252 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 253 | <code>.archive-progress[hidden] { display: none !important; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 254 | <code>.archive-progress-head {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 255 | <code>  display: flex;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 256 | <code>  justify-content: space-between;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 257 | <code>  gap: 12px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 258 | <code>  align-items: center;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 259 | <code>  margin-bottom: 9px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 260 | <code>  color: #30363c;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 261 | <code>  font-size: 13px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 262 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 263 | <code>#archiveProgressPercent { color: #6b747c; font-variant-numeric: tabular-nums; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 264 | <code>.archive-progress-track {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 265 | <code>  height: 7px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 266 | <code>  overflow: hidden;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 267 | <code>  border-radius: 999px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 268 | <code>  background: #e7eaed;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 269 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 270 | <code>.archive-progress-fill {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 271 | <code>  width: 0%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 272 | <code>  height: 100%;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 273 | <code>  border-radius: inherit;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 274 | <code>  background: #35a8df;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 275 | <code>  transition: width .16s linear;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 276 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 277 | <code>.archive-progress-bytes {</code> | 开始一个 CSS 选择器规则，后续属性作用于匹配元素。 | Starts a CSS selector rule; following properties apply to matching elements. |
| 278 | <code>  min-height: 16px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 279 | <code>  margin-top: 7px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 280 | <code>  color: #8a9299;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 281 | <code>  font-size: 11px;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 282 | <code>  font-variant-numeric: tabular-nums;</code> | 为当前 CSS 规则设置一个样式属性及其值。 | Sets a style property and value for the current CSS rule. |
| 283 | <code>}</code> | 结束当前 CSS 选择器或条件规则。 | Ends the current CSS selector or conditional rule. |
| 284 | &nbsp; | 空行：用来分隔相邻的逻辑或视觉区域，不执行任何操作。 | Blank line: separates neighboring logic or visual sections and performs no operation. |
| 285 | <code>.archive-progress.error { border-color: #efb7b7; background: #fff8f8; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
| 286 | <code>.archive-progress.error .archive-progress-fill { background: #d95c5c; }</code> | 组成当前 CSS 样式规则的选择器、属性或格式片段。 | Forms a selector, property, or formatting fragment of the current CSS rule. |
